| Original Text | Revised Text | Changes |
|---------------|--------------|---------|
| **Контекст:** администратор уже вошёл, выбрал активную доску. | **Контекст:** администратор уже вошёл и выбрал активную доску. | Добавлен союз между однородными сказуемыми. |
| **Путь:** добавляет карточку в колонку «к выполнению» → начинает задачу и перетаскивает карточку в «в работе» (мышь / касание / клавиатура за drag-handle) → по завершении перетаскивает в «готово».<br><br>Администратор может переместить Карточку в другую Колонку и на нужную Позицию мышью, касанием или клавиатурой за drag-handle. | **Путь:** добавляет карточку в колонку «к выполнению» → начинает задачу и перетаскивает карточку в «в работе» (мышью, касанием или клавиатурой с помощью drag-handle) → по завершении перетаскивает в «готово».<br><br>Администратор может переместить Карточку в другую Колонку и на нужную Позицию мышью, касанием или клавиатурой с помощью drag-handle. | Исправлено нарушение управления в повторяющейся конструкции; унифицировано перечисление способов взаимодействия (UJ-2 и FR-14). |
| **Кульминация:** Карточка немедленно перемещается в UI оптимистически, а сервер сохраняет перемещение и нормализует Позиции; при ошибке UI откатывает изменение. | **Кульминация:** UI немедленно и оптимистически отображает перемещение Карточки, а сервер сохраняет перемещение и нормализует Позиции; при ошибке UI откатывает изменение. | Устранено неясное присоединение наречия «оптимистически»: оно относится к отображению перемещения в UI, а не к физическому перемещению Карточки. |
| Имя уникально в пределах автора.<br><br>Имя уникально в пределах автора; новая Доска сразу получает три системные Колонки.<br><br>Имя Доски уникально без учёта регистра в пределах автора; повторное имя отклоняется с конфликтом (409).<br><br>доменные инварианты (три Статуса, уникальность имени Доски на автора, нормализация Позиций) обеспечиваются на сервере. | Имя уникально в пределах Администратора.<br><br>Имя уникально в пределах Администратора; новая Доска сразу получает три системные Колонки.<br><br>Имя Доски уникально без учёта регистра в пределах Администратора; повторное имя отклоняется с конфликтом (409).<br><br>доменные инварианты (три Статуса, уникальность имени Доски в пределах Администратора, нормализация Позиций) обеспечиваются на сервере. | Заменён не определённый в Глоссарии термин «автор» на установленный термин «Администратор» во всех четырёх местах. |
| Во время уже открытой страницы централизованного перехвата 401 нет: ошибка отображается в контексте текущей операции до перезагрузки или повторного входа. | При работе с уже открытой страницей централизованного перехвата ошибок 401 нет: ошибка отображается в контексте текущей операции до перезагрузки или повторного входа. | Исправлена неестественная временная конструкция; уточнено, что 401 — тип перехватываемой ошибки. |
| На чистой БД после старта существует ровно одна учётная запись Администратора и роль `ADMIN`. | На чистой БД после старта существуют ровно одна учётная запись Администратора и одна роль `ADMIN`. | Устранена двусмысленность области действия слова «ровно» и согласовано сказуемое с составным подлежащим. |
| Применяются те же правила непустого имени, максимума 120 символов и уникальности без учёта регистра, что и при создании. | Применяются те же требования к имени, что и при создании: оно должно быть непустым, содержать не более 120 символов и быть уникальным без учёта регистра. | Исправлена грамматически неоднородная конструкция «правила … максимума» без изменения требований. |
| Попытка сохранить набор Колонок не из ровно трёх системных Статусов отклоняется (400). | Попытка сохранить набор Колонок, который не содержит ровно три системных Статуса, отклоняется (400). | Исправлена неясная конструкция «набор не из ровно трёх». |
| Отображаемое имя и позиция Колонок обновляются; Статусы остаются неизменными. | Отображаемые имена и позиции Колонок обновляются; Статусы остаются неизменными. | Согласовано число: обновляются имена и позиции нескольких Колонок. |
| Обновлённые заголовок/описание сохраняются и возвращаются клиенту. | Обновлённые заголовок и описание сохраняются и возвращаются клиенту. | Заменён неоднозначный косой слеш союзом в перечне двух полей. |
| Перемещение задаётся целевым Статусом и целевым индексом; сервер атомарно применяет перемещение и нормализует Позиции. | Перемещение задаётся целевым Статусом и целевой Позицией; сервер атомарно применяет перемещение и нормализует Позиции. | Заменён синоним «индекс» на определённый в Глоссарии термин «Позиция». |
| Мутации Досок и Карточек выполняются под проверкой владения с блокировкой на запись, чтобы конкурентные операции не нарушали Позиции. | Мутации Досок и Карточек выполняются с проверкой владения и блокировкой записи, чтобы конкурентные операции не нарушали Позиции. | Исправлено управление и прояснена однородность двух условий выполнения мутаций. |
| Нет саморегистрации, смены или восстановления пароля через продукт, OAuth/SSO — только bootstrap-администратор с учётными данными из конфигурации. | Нет саморегистрации, смены или восстановления пароля через продукт и входа через OAuth/SSO — доступен только bootstrap-администратор с учётными данными из конфигурации. | Восстановлен параллелизм перечисления: OAuth/SSO обозначен как способ входа; добавлено сказуемое во второй части. |
| Нет функций экспорта, импорта и резервного восстановления данных; резервное копирование относится к эксплуатации развёртывания. | Нет функций экспорта, импорта и восстановления данных из резервной копии; резервное копирование относится к эксплуатации развёртывания. | Заменено неясное сочетание «резервное восстановление» общепринятым обозначением операции. |
| секреты — из окружения, не в коде; наружу из служебных эндпоинтов открыт только health. | секреты — из окружения, не в коде; из служебных эндпоинтов извне доступен только health-эндпоинт. | Исправлены неясное наречие «наружу» и смешение термина `health` с подразумеваемым словом «эндпоинт». |
| политика доступа, CSRF/session policy и bootstrap-Администратор являются частью текущего контракта | Consider: политика доступа, политика CSRF и Сессий и bootstrap-Администратор являются частью текущего контракта? | Смешанная англо-русская конструкция с косым слешем неясна; предложено проверить, означает ли `CSRF/session policy` две политики — CSRF и Сессий. |
