---
topic: BMAD Todo List — Kanban с JWT-админом
updated: 2026-07-22T14:17
---

- (decision) Intent=Create brownfield reverse-PRD: документируем реализованное as-is
- (decision) Stakes=internal tool (~5-8 стр); working mode=Fast path
- (decision) Scope=as-is + Non-Goals по факту; будущие идеи в Open Questions не добавляем
- (event) Источники: README.md, docs/*, project-context.md, исходники backend+frontend
- (event) Черновик PRD написан: 11 FR по 4 фичам, spine + Cross-Cutting NFRs + Constraints
- (assumption) TTL JWT-сессии не фиксируется в PRD (Open Q1); JWT в Authorization header, не в localStorage/URL
- (decision) JWT TTL=1 час по умолчанию, настраивается JWT_EXPIRATION_MS; при bootstrap UI невалидный токен очищается и ProtectedRoute ведёт на login; активные 401 централизованно не перехватываются
- (decision) Прикладных лимитов числа досок/карточек нет; предел задают ресурсы БД и развёртывания
- (decision) Экспорт/восстановление не реализованы; backup относится к эксплуатации и вне MVP
- (change) Все Open Questions и ASSUMPTION в PRD закрыты по фактической реализации
- (change) Reconciliation: исправлено расхождение optimistic move, добавлены лимиты полей, UX confirm/автовыбор и фактические edge cases
- (decision) FR досок разделён; итоговая нумерация FR-1..FR-15; health получил отдельный FR-15
- (decision) PRD имеет приоритет для as-is поведения move; project-context сохраняет желаемое правило будущей синхронизации с API
- (event) Memlog audit complete: все решения отражены в PRD, неподтверждённых допущений нет
- (event) Reviewer gate passed: critical=0, high=0; structural and prose polish applied
- (event) PRD finalized
- (change) Параметры пользователей на карточках: addendum.md создан (PRD Addendum, on_addendum). Дельта: FR-11 +assignee, FR-12 +assignee, Non-Goals §5 — снято ограничение 'назначения исполнителей', глоссарий +Creator/+Assignee, 8 AC, 8 EC, 6 OQ
