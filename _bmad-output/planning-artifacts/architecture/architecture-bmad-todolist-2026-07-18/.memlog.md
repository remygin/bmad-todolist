---
scope: BMAD Todo List — full system brownfield
purpose: Ratify existing architecture invariants for epics/stories consistency
altitude: initiative→features/epics
updated: 2026-07-23T21:45
---

- (decision) Mode=Fast path; deliverable=spine only; brownfield ratify-from-code
- (constraint) PRD final: FR-1..15, 3 statuses, ADMIN-only, optimistic move as-is, field max lengths
- (constraint) project-context.md: Java17 SpringBoot4 JWT sessionStorage Controller-Service-Repository Flyway validate 3 columns
- (direction) Altitude=initiative→features/epics; purpose=build-substrate for epics/stories consistency
- (decision) AD-1 Paradigm=[ADOPTED] Layered BE (Controller→Service→Repository) + component SPA FE; packages auth/board/card/user/config/common and pages/components/api/auth — prevents mixed layering and dumping logic in controllers/UI
- (decision) AD-2 Dependency=[ADOPTED] FE→BE only via /api JSON+Bearer; BE never depends on FE; domain packages talk through Service not Controller↔Controller — prevents dual HTTP clients and cyclic FE/BE coupling
- (decision) AD-3 Auth=[ADOPTED] Stateless JWT Bearer; store sessionStorage todolist.accessToken not localStorage/cookie/URL; Kanban @PreAuthorize hasRole(ADMIN); public only login+health — prevents auth drift and role/authority confusion
- (decision) AD-4 Column invariant=[ADOPTED] Board always exactly 3 columns statuses TODO|IN_PROGRESS|DONE; rename/reorder only; no add/remove/replace status — prevents custom status sprawl
- (decision) AD-5 Ownership+lock=[ADOPTED] Boards/cards scoped to author; mutations via findOwned*ForUpdate pessimistic lock; foreign ids → 404 not 403 — prevents cross-tenant leaks and race on positions
- (decision) AD-6 Positions=[ADOPTED] Server normalizes card position after create/move/delete; move uses targetStatus+targetIndex; FE may optimistic-update with rollback (as-is PRD) — prevents dual sources of position truth on server
- (decision) AD-7 Schema=[ADOPTED] Flyway V* only; ddl-auto=validate; never edit applied migrations — prevents schema drift
- (decision) AD-8 Errors+contracts=[ADOPTED] ErrorResponse message+details; statuses via ApiExceptionHandler; ColumnStatus mirrored FE/BE; API types in frontend/src/api next to functions — prevents envelope/status drift
- (decision) AD-9 FE HTTP=[ADOPTED] All HTTP through apiRequest in client.ts; no fetch from pages/components; no axios/React Query without task — prevents parallel HTTP stacks
- (decision) AD-10 Ops envelope=[ADOPTED] Compose: postgres16 + backend + frontend nginx; secrets from env; actuator only health exposed; dual ports local 8081 vs compose 8080 — prevents inventing alt deploy topology
- (version) Stack pinned from pom/package/docs: Java17 SpringBoot4.0.7 JJWT0.12.6 React18.3 TS~5.6 Vite5.4 RRD6.30 dnd-kit PostgreSQL16 nginx1.27 Node20
- (assumption) [ASSUMPTION] CORS * acceptable for current single-operator self-host; production origin narrowing deferred
- (decision) Deferred: CI/CD pipelines, multi-user roles, custom statuses, export/backup product features, tightening CORS, FE sync after move to match project-context intent
- (change) Gate fixes: AD-3 CSRF+STATELESS; AD-10 CORS freeze+alpine; AD-11 aggregates; AD-12 nested BoardResponse; AD-4 position vs status; AD-6 exclusive CardService writes + FE sync split
- (event) lint_spine + reviewer gate applied; spine ready to finalize
- (change) AD-7 cascade FK; DnD convention @dnd-kit; UX FR gaps deferred to PRD
- (event) spine finalized
- (event) spine finalized
- (event) spine finalized
- (event) spine finalized
- (event) delta promotion: assignee/creator feature — 2026-07-23
- (decision) AD-3 amended: GET /api/boards and GET /api/boards/{id} — access ADMIN OR assignee (EXISTS cards.assignee_id), no @PreAuthorize; mutations remain ADMIN-only; GET /api/users is @PreAuthorize ADMIN
- (decision) AD-5 amended: Board read access extended — author_id OR EXISTS cards.assignee_id; mutations remain author-scoped
- (decision) AD-13 new: GET /api/users — ADMIN-only, returns id+username, no pagination MVP
- (decision) AD-14 new: Card.creator NOT NULL + Card.assignee nullable — LAZY + @EntityGraph, loading inside @Transactional
- (decision) AD-15 new: User deletion — SET NULL assignee_id, prohibit if has creator_id references
