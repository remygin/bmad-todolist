---
scope: feature: assignee и creator на карточку
purpose: build-substrate
altitude: feature
updated: 2026-07-23T21:45
---

- (decision) AD-D1 — creator/assignee поля на Card: ManyToOne → User, creator NOT NULL, assignee nullable, FetchType.LAZY + @EntityGraph('Card.withCreatorAndAssignee') в CardService
- (decision) AD-D2 — Доступ к Доске: проверка author_id OR EXISTS cards.assignee_id через @Query в BoardRepository; без отдельной board_members таблицы
- (decision) AD-D3 — GET /api/users: только ADMIN, возвращает id + username (без пароля/email/роли), без пагинации для MVP
- (decision) OQ-1 (BP-4) resolved: Исполнитель видит Доску целиком — все карточки, включая чужие. Соответствует AC-6.2.
- (decision) OQ-2 (BP-5) resolved: Доступ теряется немедленно при снятии последнего assignee — EXISTS на каждый запрос, без кэширования
- (decision) OQ-4 resolved: SET NULL при удалении пользователя для assignee_id; запрет удаления для creator (всегда реальный пользователь)
- (decision) OQ-8 resolved: FE определяет ADMIN vs исполнитель через roles из JWT (hasRole('ADMIN') → true/false), без отдельного endpoint
- (decision) OQ-10 resolved: move-операция не затрагивает assignee — payload (targetStatus + targetIndex) остаётся без изменений
- (decision) OQ-3 resolved: Выбор assignee в UI — выпадающий список всех пользователей (GET /api/users); без автодополнения для MVP, но GET /api/users закладывает query param ?q= для будущего поиска
- (decision) Flyway V3: ALTER cards ADD creator_id BIGINT NOT NULL REFERENCES users(id), ADD assignee_id BIGINT REFERENCES users(id); бэкфилл существующих записей WHERE creator_id IS NULL → id bootstrap-админа (поиск по ROLE_ADMIN), затем SET NOT NULL
- (assumption) OQ-5 (precondition): создание пользователей — out of scope фичи; требуется POST /api/users или SQL-скрипт до реализации. Архитектура дельты предполагает, что минимум 2 пользователя уже существуют.
